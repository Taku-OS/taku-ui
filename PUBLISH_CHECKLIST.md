# 发布检查清单

## ⚠️ 重要：仓库可见性设置

你的仓库 `Taku-OS/taku-ui` 是**私有的**，这会导致：
- ❌ `raw.githubusercontent.com` 无法访问（返回 404）
- ❌ 用户无法直接使用 `npx taku-ui@latest add`（除非设置 token）

## 🔓 解决方案

### 方案 1: 改为公开仓库（推荐，如果组件库是开源的）

1. 访问 GitHub 仓库设置：
   ```
   https://github.com/Taku-OS/taku-ui/settings
   ```

2. 滚动到页面底部，找到 **"Danger Zone"** 部分

3. 点击 **"Change repository visibility"**

4. 选择 **"Make public"**

5. 确认更改

**优点**：
- ✅ 用户可以直接使用，无需配置 token
- ✅ Registry 可以直接访问
- ✅ 更符合开源组件库的定位

**缺点**：
- ⚠️ 代码完全公开

### 方案 2: 保持私有 + 使用 GitHub Token（当前方案）

如果必须保持私有，用户需要：

1. 生成 GitHub Token（见 `PRIVATE_REPO_SETUP.md`）
2. 设置环境变量：
   ```bash
   export TAKU_UI_GITHUB_TOKEN=ghp_xxxxxxxxxxxx
   ```
3. 使用 CLI：
   ```bash
   npx taku-ui@latest add window-controls
   ```

**优点**：
- ✅ 代码保持私有
- ✅ 可以控制访问权限

**缺点**：
- ❌ 用户体验较差（需要配置 token）
- ❌ 不适合大规模分发

## 📋 发布前检查

- [ ] Registry 文件已推送到 GitHub
- [ ] 仓库可见性设置已确定（公开/私有）
- [ ] CLI 已构建（`packages/cli/dist/index.js` 存在）
- [ ] npm 包名可用（`taku-ui` 已确认可用）
- [ ] npm 账户已登录

## 🚀 发布步骤

### 如果选择公开仓库：

```bash
# 1. 在 GitHub 上将仓库改为公开（见上方）

# 2. 验证 Registry 可访问
curl https://raw.githubusercontent.com/Taku-OS/taku-ui/main/registry/index.json

# 3. 发布 CLI
cd packages/cli
npm login
npm publish
```

### 如果保持私有：

```bash
# 1. 发布 CLI（用户需要配置 token）
cd packages/cli
npm login
npm publish

# 2. 在文档中说明需要配置 token（见 PRIVATE_REPO_SETUP.md）
```

## ✅ 发布后验证

```bash
# 测试安装（如果是公开仓库）
npx taku-ui@latest init
npx taku-ui@latest add window-controls

# 如果是私有仓库，需要先设置 token
export TAKU_UI_GITHUB_TOKEN=ghp_xxxxxxxxxxxx
npx taku-ui@latest add window-controls
```

## 💡 建议

对于组件库，**强烈建议使用公开仓库**，因为：
1. 更好的用户体验（无需配置）
2. 更符合开源组件库的定位
3. 更容易推广和使用
4. Registry 可以直接访问

如果担心代码泄露，可以考虑：
- 只公开 `registry/` 目录
- 使用单独的公开仓库存放 Registry
- 组件代码本身是复制到用户项目的，用户拥有代码所有权

